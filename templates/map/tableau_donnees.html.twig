{% extends 'nouvellepage/ruches_privees.html.twig' %}
{% block tableau_donnees %}
<!-- for HTML5 -->
<meta charset="UTF-8">
    <center>
    <br></br>
    {% if apiculteurs %}
        <h1>Mes ruches présents dans la région {{ region }}</h1>
       
        <table border bgcolor="white" cellpadding="5px">
        <tr>
        	<td>Nom ruche</td>
        	<td>Etat</td>
          	<td>Date d'installation</td>
        	<td>Visibilité</td>
        </tr>
        {% for apiculteur in apiculteurs %}
		<tr> 
			<td><a href="{{ path('info_ruche',{nomruche: apiculteur.ruche.nomruche  }) }}"> {{ apiculteur.ruche.nomruche }}			</a></td>
			<td> {{ apiculteur.ruche.etat }}</td>
			<td> {{ apiculteur.ruche.dateinstall|date("m/d/Y") }}			</td>
			<td> {{ apiculteur.ruche.visibilite }}							</td>
		</tr>
        {% endfor %}
        </table>
        {% else %}
         <h1>Aucune ruche dans la région choisie</h1>
        {% endif %}
        <br></br>
       
    </center>
{% endblock %}