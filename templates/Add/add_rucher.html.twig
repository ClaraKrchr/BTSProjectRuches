{% extends 'base.html.twig' %}
{% block title %} Ajouter un Rucher{% endblock %}
{% block body %}

 {% for message in app.flashes('success') %}
      		 <center><div class="alert alert-success">
        	    {{ message }}
			 </div></center>
     	  {% endfor %}

<meta charset="UTF-8">
<link rel="stylesheet" href="{{ asset('css/inscription.css') }}">
	<center><h1>Ajouter un rucher</h1></center>
	<br>
	<div class="d-flex justify-content-around">
		
    		{{ form_start(addrucherform) }}
       		{{ form_widget(addrucherform) }}	
           		
       			<button type="submit" class="btn btn-primary">Valider</button>
       		
            {{ form_end(addrucherform) }}
            {% for message in app.flashes('notice') %}
              	<div class="flash-notice">
                	Chargement effectué
  	    		</div>
  	    	{% endfor %}
    	
    	
	<br>
	<div class="row">
			<div id="block2" class="col-lg-4">
			<center>
				<table class="table table-bordered" >
                	<tr>
                		<td>Latitude</td>
                		<td>Longitude</td>
                		<td>Région</td>
                	</tr>
                	<tr>
                	    <td> {{ latitude }}						</td>
                	    <td> {{ longitude }}					</td>
                	    <td> {{ region }}					</td>
                	</tr>
                </table>
                <br></br>
            </center>
			</div>
		</div>
		<a href="{{ path ('googleMap') }}"><button class="btn btn-primary">Afficher la carte </button></a>
	</div>
{% endblock %}